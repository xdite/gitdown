EDIT
<%= form_for :wiki, :url => page_path(@wiki.name), :html => { :method => :put }  do |f| -%>
    <%= f.hidden_field :name %>
    <%= f.text_area :content %>
    <%= f.submit "Submit", :disable_with => 'Submiting...' %>
<% end -%>